# 润色与打包指南（汉化包工作流）

**适用阶段**：游戏内汉化已生效（08 册严格模式），进入"持续润色 + 打包分发"阶段。基准 buildid 25436400 / 2026-09-22。

## 1. 润色循环（改词即生效）

**唯一入口文件：[润色\润色词典.csv](润色/润色词典.csv)**，三列：`类型,键,中文`

| 类型 | 键 | 作用域 | 生效方式 |
|---|---|---|---|
| `gcl` | 界面英文原文（精确匹配，含大小写/###占位符） | gcl 内嵌串替换（最高优先级，覆盖自动/紧凑层） | `python 工具\patch_gcl_strings.py apply` |
| `name` | 定义表条目 id（如 `black_bear`） | JSON name 汉化（优先于 TSV 映射/内置生物表） | `python 工具\json_name_zh.py apply` |

约束与提示：
- gcl 通道受**等长字节替换**限制：中文字节数（=字数×3）不得超过原英文字节数；超出会自动跳过并计入 status 报告。
- 想系统排查可润色项：跑 `patch_gcl_strings.py status`，它会重建 [润色\超长待译.csv](润色/)（当前 146 条：官方译超预算待短译 + 5 字节短词/4 字节单词[放弃区：单字中文观感差]）。
- name 通道无长度限制。
- 示例行见词典文件头两行（`gcl,Drag Person,拖人` / `name,black_bear,黑熊`）。

## 2. 打包流程

`python 工具\打包汉化包.py` 一键完成：

1. **生成 [汉化包\安装汉化.py](../汉化包/)**——自包含整合安装器（词典从 工具\ 两脚本实时导入内嵌，始终同源）：
   - 自动定位 Steam 库（注册表 SteamPath + libraryfolders.vdf，无需配置路径）；
   - `install`：①TSV en:=zh ②字体 ③JSON name ④gcl 严格替换（含长度前缀池签名校验）；自备份到安装包旁 `备份_<buildid>\` 与 `备份_tsv\`；幂等，游戏更新后重跑即可；
   - `status` / `uninstall`（按备份完整还原）；
   - **版权干净**：翻译对在玩家本机从其游戏文件 zh 列推导，包内只有脚本与人工短译词（zip 仅 ~12KB，零游戏文本）——比 03 发布指南的 diff-only 原则更彻底。
2. 复制 `润色词典.csv`（润色随包分发，玩家也能改词）。
3. 生成 `使用说明.md` 并压 zip 到 `补丁\Anymaker简中汉化包_b<buildid>_<日期>.zip`。
4. 已测试 buildid 记录在安装器 `KNOWN_BUILDS`（25422107/25436400）；新版本按内容推导兼容，异常先 uninstall。

## 3. 发布建议（衔接 03 发布指南）

- GitHub：仓库放本包三件套 + 工具源码 + 润色词典；release 按 buildid 打 tag。
- 任何渠道分发 zip 即可；玩家只需 Python 3.8+。
- 官方一旦在 Steam 语言列表启用简中：uninstall 后直接属性切中文（数据同源，体验更佳），本包退役为"字体/短词补差"可选件。

## 4. 首轮润色记录（2026-09-22，参照 Stormworks 社区汉化）

对照源：BKN46 `language.tsv`（8813 行，工坊物品 2019972792 本机副本）。Anymaker 组件+物品 969 条中与 SW 精确同词 210 条：**一致 116（同厂术语血统天然吻合）**，分歧 94。决策：

- **采纳 SW（11 条）**：罗盘传感器、电力继电器、面罩/防毒面具、绳索、望远镜、高可视工作服、一次性防护服；另修正官方 LLM 错误映射 2 条（chef_skullcap/scrubs_hat 的 "Cap" 被统一译成"鸭舌帽"→厨师帽/手术帽）。
- **保留 AM（多数）**：家具具名（躺椅/大床/台灯/纸篓）优于 SW 泛称；空气滤清器（SW"气体过滤端口"是另一物件）、板条箱（SW"机箱"是另一物件）、刻度盘（SW"刻度表"）、麦克风、针织帽（SW"无檐小便帽"）、长裤/工作服（AM 更准）。
- **刻意不采纳（防不一致）**：节流阀/制动器——gcl 通道字节预算放不下（节流阀 9B>Throttle 8B），若 JSON 采用会造成 gcl 与列表两处译名打架；保留官方 油门/刹车。
- **勘误落地（05 §2.4，经 tsv 通道）**：ROLL 轴绑定 滚动→横滚、"按下 键重新绑定"→"按下按键重新绑定"、manual_1"感到受到"句、manual_55 句构（参照官方繁体）。
- 说明：gcl 中残留的"Engine→引擎"来自官方 items 表对同名物品的官方译名（经 auto 层），非我方杜撰，保留官方措辞。
- 工程改动：zh_overwrite.py 新增 tsv 覆盖通道（id→en/zh 双列改写）；备份\rom_b25436400 已补齐全部纯净原件（TSV/字体/JSON/gcl 共 11 项+指纹，此前 JSON 误备了已译版本，已用 25422107 原件覆盖修正）。

## 5. 当前边界（2026-09-22）

- gcl 残留英文 146 条：约半数为 5/4 字节短词（MUSIC/MOUSE/BACK/JUMP 等放不下双字中文，刻意放弃）、其余见超长待译.csv 可继续短译；
- `QUALITY→画质` 等个别词条因在 gcl 中无合法锚点（既非 NUL 界定也无长度前缀签名）未生效，留观；
- F7 调试面板大量 dev 串未译（低价值，需要时向 MANUAL 词典加词）。
